% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/camera.R
\name{.run_camera}
\alias{.run_camera}
\title{Competitive gene-set enrichment with CAMERA}
\usage{
.run_camera(
  expr,
  design,
  contrast,
  id_type = NULL,
  pAdjustMethod = "BH",
  chemicals_meta,
  cache_dir,
  ...
)
}
\arguments{
\item{expr}{Numeric expression matrix (genes x samples).
\code{rownames(expr)} must be Entrez IDs or HGNC symbols matching the
cached CTD gene sets.}

\item{design}{Design matrix produced e.g. by
\code{\link[stats]{model.matrix}}.}

\item{contrast}{Either a column number or column name of \code{design}, or
a numeric contrast vector.}

\item{id_type}{Either \code{"entrez"}, \code{"symbol"}, or \code{NULL} for
auto-detection from \code{rownames(expr)}.}

\item{pAdjustMethod}{Method passed to \code{\link[stats]{p.adjust}} to
compute the \code{padj} column.}

\item{chemicals_meta}{Data frame with columns \code{ChemicalID} and
\code{ChemicalName}, used to annotate results.}

\item{cache_dir}{Directory holding the cached CTD \code{.rda} files.}

\item{...}{Forwarded to \code{\link[limma]{camera}}
(e.g. \code{inter.gene.cor}, \code{use.ranks}, \code{allow.neg.cor},
\code{trend.var}).}
}
\value{
A data frame with columns \code{ChemicalID}, \code{ChemicalName},
  \code{NGenes}, \code{Direction}, \code{Correlation} (if reported),
  \code{pvalue}, \code{padj}, sorted by \code{padj} ascending.
}
\description{
Internal function that runs the CAMERA competitive gene-set test
(\code{\link[limma]{camera}}) of an expression matrix against the cached
CTD chemical gene sets. Unlike ORA/GSEA --- which take a single
(ranked) gene list --- CAMERA tests, for each chemical, whether its
target genes show a stronger differential signal than the rest of the
transcriptome under a user-supplied design and contrast, while accounting
for inter-gene correlation.
}
\keyword{internal}
