# Validate that an expression matrix has usable rownames

Validate that an expression matrix has usable rownames

## Usage

``` r
.validate_expr_matrix(x)
```

## Arguments

- x:

  A numeric matrix.
