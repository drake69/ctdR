# Run ORA branch of enrichment analysis

Run ORA branch of enrichment analysis

## Usage

``` r
.run_ora(entrez_ids, ChemicalName_GeneSymbols, chemicals, pAdjustMethod)
```

## Value

A data frame of ORA enrichment results.
